# frozen_string_literal: true

RSpec.describe FlowState do
  it "has a version number" do
    expect(FlowState::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
